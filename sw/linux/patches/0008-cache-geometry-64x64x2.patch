diff --git a/arch/loongarch/include/asm/cacheflush.h b/arch/loongarch/include/asm/cacheflush.h
index 61718bbae..e89b16b59 100644
--- a/arch/loongarch/include/asm/cacheflush.h
+++ b/arch/loongarch/include/asm/cacheflush.h
@@ -137,6 +137,29 @@ static inline void flush_dcache_line(unsigned long addr)
          : "r" (base),                       \
            "i" (op));
 
+// 2026-final: 本核 cache 行为 64B，补齐 64B 步长 unroll（32 cacop 覆盖 2KB）
+#define cache64_unroll32(base,op)                   \
+     __asm__ __volatile__(                       \
+     "   cacop %1,%0, 0x000; cacop %1, %0,0x040  ;  \n" \
+     "   cacop %1,%0, 0x080; cacop %1, %0,0x0c0  ;  \n" \
+     "   cacop %1,%0, 0x100; cacop %1, %0,0x140  ;  \n" \
+     "   cacop %1,%0, 0x180; cacop %1, %0,0x1c0  ;  \n" \
+     "   cacop %1,%0, 0x200; cacop %1, %0,0x240  ;  \n" \
+     "   cacop %1,%0, 0x280; cacop %1, %0,0x2c0  ;  \n" \
+     "   cacop %1,%0, 0x300; cacop %1, %0,0x340  ;  \n" \
+     "   cacop %1,%0, 0x380; cacop %1, %0,0x3c0  ;  \n" \
+     "   cacop %1,%0, 0x400; cacop %1, %0,0x440  ;  \n" \
+     "   cacop %1,%0, 0x480; cacop %1, %0,0x4c0  ;  \n" \
+     "   cacop %1,%0, 0x500; cacop %1, %0,0x540  ;  \n" \
+     "   cacop %1,%0, 0x580; cacop %1, %0,0x5c0  ;  \n" \
+     "   cacop %1,%0, 0x600; cacop %1, %0,0x640  ;  \n" \
+     "   cacop %1,%0, 0x680; cacop %1, %0,0x6c0  ;  \n" \
+     "   cacop %1,%0, 0x700; cacop %1, %0,0x740  ;  \n" \
+     "   cacop %1,%0, 0x780; cacop %1, %0,0x7c0  ;  \n" \
+         :                           \
+         : "r" (base),                       \
+           "i" (op));
+
 /* build blast_xxx, blast_xxx_page, blast_xxx_page_indexed */
 #define __BUILD_BLAST_CACHE(pfx, desc, indexop, hitop, lsize, extra)    \
  static inline void extra##blast_##pfx##cache##lsize(void)       \
@@ -183,6 +206,9 @@ static inline void extra##blast_##pfx##cache##lsize##_page_indexed(unsigned long
  __BUILD_BLAST_CACHE(i, icache, Index_Invalidate_I, Hit_Invalidate_I, 16, )
  __BUILD_BLAST_CACHE(d, dcache, Index_Writeback_Inv_D, Hit_Writeback_Inv_D, 32, )
  __BUILD_BLAST_CACHE(i, icache, Index_Invalidate_I, Hit_Invalidate_I, 32, )
+ // 2026-final: 64B 行实例（本核实际几何）
+ __BUILD_BLAST_CACHE(d, dcache, Index_Writeback_Inv_D, Hit_Writeback_Inv_D, 64, )
+ __BUILD_BLAST_CACHE(i, icache, Index_Invalidate_I, Hit_Invalidate_I, 64, )
 
 /* build blast_xxx_range, protected_blast_xxx_range */
  #define __BUILD_BLAST_CACHE_RANGE(pfx, desc, hitop, prot, extra)    \
diff --git a/arch/loongarch/mm/cache.c b/arch/loongarch/mm/cache.c
index 6033ec472..5df78cac8 100644
--- a/arch/loongarch/mm/cache.c
+++ b/arch/loongarch/mm/cache.c
@@ -98,7 +98,7 @@ static void la32_dma_cache_wback_inv(unsigned long addr, unsigned long size)
      BUG_ON(size == 0);
 
      if (size >= dcache_size) {
-         blast_dcache16();
+         blast_dcache64(); // 2026-final: 64B line
      } else {
          blast_dcache_range(addr, addr + size);
      }
@@ -110,7 +110,7 @@ static void la32_dma_cache_inv(unsigned long addr, unsigned long size)
     BUG_ON(size == 0);
 
     if ( size >= dcache_size) {
-        blast_dcache16();
+        blast_dcache64(); // 2026-final: 64B line
     } else {
         unsigned long lsize = cpu_dcache_line_size();
         unsigned long almask = ~(lsize - 1);
@@ -130,7 +130,11 @@ static void probe_pcache(void)
 	unsigned int lsize, sets, ways;
 	unsigned int config;
 
-	config = 0xfe994cd3;
+	// 2026-final: match NOP core geometry 64sets x 64B x 2way
+	// 解码: I: linesz=2<<((c>>19)&7) sets=64<<((c>>22)&7) ways=1+((c>>16)&7)
+	//       D: linesz=2<<((c>>10)&7) sets=64<<((c>>13)&7) ways=1+((c>>7)&7)
+	// 原值 0xfe994cd3 = 16B/256sets/2way 与本核(64B/64sets/2way)不符
+	config = 0xfe2914d3;
 
 	lsize = (config >> 19) & 7;
 	sets  = 1 << ((config & CPUCFG17_L1I_SETS_M) >> CPUCFG17_L1I_SETS);
@@ -164,6 +168,12 @@ static void probe_pcache(void)
                                           c->dcache.ways *
                                           c->dcache.linesz;
 	c->dcache.waysize = dcache_size / c->dcache.ways;
+
+	// 2026-final: waybit 此前全树从未赋值（恒 0），blast_*cache 全清的
+	// way 迭代 ws_inc=1<<waybit=1，只能覆盖 way 0；按 waysize 计算
+	// （4096B -> waybit=12，way 选择位为 addr[12]，与 index=addr[11:6] 衔接）
+	c->icache.waybit = ffs(icache_size / c->icache.ways) - 1;
+	c->dcache.waybit = ffs(dcache_size / c->dcache.ways) - 1;
 	c->options |= LOONGARCH_CPU_PREFETCH;
 
 	pr_info("Primary instruction cache %ldkB, %s, %s, linesize %d bytes.\n",
