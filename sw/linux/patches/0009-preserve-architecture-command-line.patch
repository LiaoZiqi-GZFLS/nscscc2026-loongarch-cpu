diff --git a/init/main.c b/init/main.c
index 5f5e3d2b8..000000000 100644
--- a/init/main.c
+++ b/init/main.c
@@ -614,7 +614,7 @@ static void __init setup_command_line(char *command_line)
 	if (extra_init_args)
 		ilen = strlen(extra_init_args) + 4; /* for " -- " */
 
-	len = xlen + strlen(boot_command_line) + 1;
+	len = xlen + strlen(command_line) + 1;
 
 	saved_command_line = memblock_alloc(len + ilen, SMP_CACHE_BYTES);
 	if (!saved_command_line)
@@ -633,7 +633,7 @@ static void __init setup_command_line(char *command_line)
 		strcpy(static_command_line, extra_command_line);
 	}
 
-	strcpy(saved_command_line + xlen, boot_command_line);
+	strcpy(saved_command_line + xlen, command_line);
 	strcpy(static_command_line + xlen, command_line);
 
 	if (ilen) {
